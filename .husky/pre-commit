# Scan staged files for secrets before anything else
# Runs on every commit — blocks if any secrets are detected
gitleaks protect --staged --redact -v

npx lint-staged
npm run typecheck
# Unit tests only — integration tests require SSH tunnel (run manually with: doppler run ... -- npm run test:integration)
npm run test:unit
